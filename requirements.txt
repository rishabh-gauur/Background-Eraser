fastapi
uvicorn
rembg  # Use [cpu] for a potentially smaller/faster deployment
Pillow
jinja2
gunicorn
python-multipart
# You may also need a serverless wrapper like mangum if deployment fails
# mangum
